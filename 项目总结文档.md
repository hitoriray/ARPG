# Unity ARPG 项目完整技术总结文档

## 📋 项目概览

### 基本信息
- **项目名称**: ARPG
- **引擎版本**: Unity 6000.3.8f1
- **开发阶段**: 客户端开发（服务端暂未涉及）
- **项目类型**: 动作角色扮演游戏（Action RPG）
- **脚本语言**: C#
- **脚本文件数量**: 354个
- **渲染管线**: URP (Universal Render Pipeline) 14.0.11

### 项目定位
这是一个专注于**打击感**和**战斗体验**的 ARPG 项目，采用现代化的 ECS 架构与传统 MonoBehaviour 混合开发模式，强调：
- 流畅的动作战斗系统
- 数据驱动的技能配置
- 高性能的 ECS 战斗逻辑
- 灵活的角色成长系统

---

## 🏗️ 核心架构设计

### 1. 混合架构模式

项目采用 **ECS + MonoBehaviour 混合架构**：

#### ECS 层（逻辑层）
- **框架**: Arch ECS
- **职责**: 战斗逻辑、伤害计算、Buff系统、碰撞检测
- **特点**: 高性能、数据驱动、逻辑与表现分离
- **核心组件**:
  - `BattleEcsRunner`: ECS 战斗入口，驱动逻辑与表现
  - `LocalBattleContext`: 战斗上下文，管理 World、System、Feature
  - `LocalLogicFeature`: 逻辑 Feature，包含所有战斗系统
  - `LocalViewFeature`: 表现 Feature，负责视图同步

#### MonoBehaviour 层（表现层）
- **职责**: 输入处理、动画控制、UI交互、场景管理
- **特点**: 易于调试、与 Unity 生态无缝集成
- **核心控制器**:
  - `PlayerController`: 玩家控制器
  - `EnemyController`: 敌人控制器（已注释，待重构）
  - `BossController`: Boss AI 控制器

### 2. 战斗系统架构

```
┌─────────────────────────────────────────────────────┐
│                   Input Layer                        │
│  (InputManager, PlayerSkillInput, InputService)     │
└──────────────────┬──────────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────────┐
│              Controller Layer                        │
│  (PlayerController, BossController)                 │
│  - 状态机驱动                                         │
│  - 动画控制 (Animancer)                              │
│  - Root Motion 处理                                  │
└──────────────────┬──────────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────────┐
│               ECS Logic Layer                        │
│  (BattleEcsRunner, Systems)                         │
│  - DamageSystem: 伤害计算                            │
│  - BuffSystem: Buff 效果处理                         │
│  - MovementSystem: 移动逻辑                          │
│  - DeathSystem: 死亡处理                             │
│  - CollisionSystem: 碰撞检测                         │
└──────────────────┬──────────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────────┐
│               View Layer                             │
│  (ViewSyncSystem, VfxSpawnSystem)                   │
│  - 位置/旋转同步                                      │
│  - 特效播放                                          │
│  - 伤害数字显示                                       │
└─────────────────────────────────────────────────────┘
```

---

## 🎮 核心系统详解

### 1. 玩家控制系统

#### PlayerController (568行)
**核心职责**:
- 状态机管理（Idle, Move, Skill, Hurt, Dead）
- 技能释放与连击
- Root Motion 处理
- ECS 实体注册

**关键特性**:
```csharp
// 双移动模式支持
- 默认模式: Animancer + Root Motion + 状态机
- Generic模式: AnimatorController + GenericPlayerLocomotionController

// 技能层管理
- Layer 0: 基础移动动画
- Layer 1: 技能动画（支持全身/上半身）
- 无缝切换，避免权重警告

// ECS 集成
- 通过 BattleEcsRunner.RegisterCharacter() 注册
- 实时同步属性到 ECS 组件
```

**状态机设计**:
- `PlayerIdleState`: 待机状态
- `PlayerMoveStartState`: 移动起步
- `PlayerMoveLoopState`: 移动循环
- `PlayerSkillState`: 技能释放（处理连击输入）
- `PlayerHurtState`: 受击状态
- `PlayerDeadState`: 死亡状态

### 2. 技能系统

#### 技能架构
```
SkillConfig (ScriptableObject)
    ├── SkillClip[] (技能片段数组，支持连击)
    │   ├── AnimationClip (动画)
    │   ├── SkillEvent[] (帧事件)
    │   │   ├── AttackDetectionEvent (攻击检测)
    │   │   ├── VfxEvent (特效)
    │   │   ├── SfxEvent (音效)
    │   │   └── BuffEvent (Buff触发)
    │   └── RootMotionConfig (根运动配置)
    └── SkillProperty (技能属性)
        ├── CD, MP消耗
        ├── 伤害倍率
        └── 连击窗口
```

**技能释放流程**:
1. `PlayerSkillInput` 检测输入并缓冲
2. `PlayerSkillBrain` 验证释放条件（CD、MP、状态）
3. `PlayerController.EnterSkillMode()` 切换动画层
4. `Animancer` 播放技能动画
5. 动画帧事件触发攻击检测/特效/音效
6. `PlayerSkillState.HandleCombatInput()` 处理连击输入
7. 技能结束后 `ExitSkillMode()` 恢复移动状态

**连击系统**:
- 支持多段连击（SkillClip 数组）
- 连击窗口机制（ComboWindow）
- 输入缓冲（Input Buffering）
- 动作取消（Cancel Window）

### 3. ECS 战斗系统

#### 核心 Systems (15个)

**逻辑系统**:
1. **ViewToLogicSyncSystem**: View → Logic 同步（玩家/Boss位置）
2. **TickSystem**: Buff Tick 计时
3. **BuffSystem**: Buff 效果应用与移除
4. **MovementSystem**: 实体移动
5. **VelocityIntegrationSystem**: 速度积分
6. **LookAtSystem**: 朝向目标
7. **ShapeDetectionSystem**: 形状碰撞检测（Box/Sphere/Capsule）
8. **WeaponDetectionSystem**: 武器碰撞检测
9. **DamageSystem**: 伤害计算与应用
10. **DeathSystem**: 死亡判定与回调
11. **DestroySystem**: 实体销毁
12. **DropLifetimeSystem**: 掉落物生命周期

**表现系统**:
13. **ViewSyncSystem**: Logic → View 同步
14. **DamageNumberSystem**: 伤害数字显示
15. **VfxSpawnSystem**: 特效生成

#### ECS 组件设计

**核心组件**:
```csharp
// 身份标识
PlayerComp, BossTag, Monster

// 空间属性
Position, Rotation, ScaleComp, Velocity

// 战斗属性
Attribute { Attack, Defense, MaxHp, MaxMp }
Health { Current, Max }

// Buff系统
BuffList (容量16)
BuffProperty { Id, Duration, Stack, Tick }
Tick { Interval, Elapsed }

// 伤害请求
DamageRequest { Value, Source, IsCrit }

// 视图引用
ViewReference { GameObject, ICharacterView }
SyncFromView (标记组件)

// 控制标记
ForbidMove, ForbidRelease, ForbidRotateOnMove
Invincible (无敌)
Death, Destroy
```

### 4. Buff 系统

#### 双层 Buff 架构

**ECS 层 Buff**:
- 高性能批量处理
- 数据驱动
- 组件: `BuffList`, `BuffProperty`, `Tick`
- System: `BuffSystem`, `TickSystem`

**MonoBehaviour 层 Buff**:
- 复杂效果（粒子、材质、动画）
- `BuffController`: Buff 管理器
- `BuffEffectResolver`: 效果解析器

**Buff 配置**:
```csharp
BuffConfig (ScriptableObject)
    ├── BuffId
    ├── Duration (持续时间)
    ├── MaxStack (最大叠加层数)
    ├── TickInterval (Tick间隔)
    ├── EffectType (效果类型)
    │   ├── 属性修改 (攻击/防御/速度)
    │   ├── 持续伤害/治疗
    │   ├── 控制效果 (眩晕/定身)
    │   └── 特殊效果 (无敌/隐身)
    └── VisualEffect (视觉效果)
```

### 5. AI 系统

#### Boss AI (Behavior Tree)
- **框架**: Behavior Designer
- **事件缓冲**: `BossAIEventBuffer` (临时事件系统)
- **核心节点**:
  - 感知节点: 检测玩家距离、血量
  - 决策节点: 选择攻击/移动/技能
  - 行为节点: 执行动作

**AI 数据配置**:
```csharp
- 感知: detectRadius, loseTargetRadius
- 追击: chaseSpeed, rotateSpeed
- 走位: preferredMinRange, preferredMaxRange, strafeSpeed
- 攻击: attackRange, attackCooldown
- 受击: hitRecoverTime
```

### 6. 属性系统

#### CharacterAttribute
**属性结构**:
```csharp
public class ModifiableValue
{
    public float BaseValue;           // 基础值
    public float FlatModifier;        // 固定加成
    public float PercentModifier;     // 百分比加成
    public float Total => (BaseValue + FlatModifier) * (1 + PercentModifier);
}

// 核心属性
- attack (攻击力)
- defense (防御力)
- maxHp (最大生命值)
- maxMp (最大魔法值)
- currentHp, currentMp
- moveSpeed (移动速度)
```

**成长系统**:
- `LevelGrowthConfig`: 等级成长曲线
- 支持属性随等级增长
- 升级事件: `DataManager.OnLevelUp`

### 7. 动画系统

#### Animancer 集成
**优势**:
- 代码驱动动画，无需复杂状态机
- 支持动态混合与过渡
- 完美支持 Root Motion

**Layer 管理**:
```csharp
Layer 0 (Base Layer):
    - Idle, Walk, Run 等移动动画
    - Weight: 1.0 (默认) / 0.0 (全身技能时)

Layer 1 (Skill Layer):
    - 技能动画
    - Weight: 0.0 (默认) / 1.0 (技能时)
    - Mask: null (全身) / upperBodyMask (上半身)
```

**Root Motion 模式**:
- `Default`: 标准 Root Motion
- `Suppressed`: 禁用 Root Motion
- `Custom`: 自定义处理（技能位移）

---

## 📦 资源与配置

### 1. 配置文件结构

```
Assets/Config/
├── Characters/              # 角色配置
│   ├── CharacterConfig      # 角色属性、动画、技能
│   ├── CharacterTable       # 角色表
│   └── CharacterLevel/      # 等级成长
├── Skill/                   # 技能配置
│   └── [角色ID]/
│       ├── Basic/           # 普攻
│       ├── Skill1/          # 技能1
│       └── Ultimate/        # 大招
├── Buff/                    # Buff配置
├── Weapon/                  # 武器配置
├── Item/                    # 道具配置
│   ├── ItemConfig
│   ├── ItemTable
│   └── LootConfig           # 掉落配置
└── EnemySpawn/              # 敌人生成配置
```

### 2. 角色配置示例

**CharacterConfig** (ScriptableObject):
```csharp
- characterId: 1001
- characterName: "安比"
- Avatar: 角色 Avatar
- PlayerSO: 动画配置
- GenericLocomotionConfig: 通用移动配置（可选）
- ControllerProfile: CharacterController 配置
- 基础属性: HP, MP, Attack, Defense
- 移动速度: WalkSpeed, RunSpeed, RotateSpeed
- LevelGrowthConfig: 成长曲线
- 默认技能列表
```

### 3. 技能配置示例

**SkillConfig**:
```csharp
- skillId: 100101
- skillName: "基础攻击1"
- skillType: Basic / Active / Ultimate
- mpCost: 0
- cooldown: 0.5f
- clips: SkillClip[]
    - animationClip: 动画片段
    - duration: 0.8f
    - comboWindow: 0.3f (连击窗口)
    - events: SkillEvent[]
        - frame: 0.3f (触发时间)
        - type: AttackDetection / Vfx / Sfx / Buff
        - config: 对应配置
```

---

## 🔧 核心管理器

### 1. GameManager
- 存档管理
- 场景切换
- 游戏流程控制

### 2. DataManager
- 存档数据持久化
- 角色进度管理
- 经验值与等级系统
- 事件: `OnLevelUp`, `OnExpGain`

### 3. InputManager / InputService
- 输入系统封装（New Input System）
- 输入缓冲
- UI 输入屏蔽

### 4. DamageNumberManager
- 伤害数字显示
- 集成 DamageNumbersPro 插件
- 实现 `IDamageNumberService` 接口

### 5. LootDropManager
- 掉落物生成
- 掉落物拾取
- 掉落配置管理

### 6. InventoryManager
- 背包系统
- 物品管理
- 装备系统

### 7. WeaponSlotManager
- 武器槽位管理
- 武器生成/销毁
- 武器碰撞检测

### 8. MapManager
- 地图管理
- 场景对象管理

### 9. EnemySpawnManager
- 敌人生成
- 波次管理
- 生成点配置

---

## 📚 第三方插件与依赖

### 核心插件

1. **Animancer** (动画系统)
   - 代码驱动动画
   - 替代 Animator Controller

2. **Odin Inspector** (编辑器增强)
   - 强大的 Inspector 扩展
   - 数据验证

3. **Behavior Designer** (行为树)
   - Boss AI 系统
   - 可视化行为树编辑

4. **DamageNumbersPro** (伤害数字)
   - 伤害数字显示
   - 丰富的视觉效果

5. **MagicaCloth2** (布料模拟)
   - 角色服装物理
   - 头发/裙摆模拟

6. **Arch ECS** (ECS框架)
   - 高性能 ECS 实现
   - 战斗逻辑核心

7. **FixMath** (定点数)
   - 确定性数学运算
   - 用于 ECS 战斗逻辑

### Unity 官方包

```json
- com.unity.addressables: 1.22.3 (资源管理)
- com.unity.cinemachine: 2.10.3 (相机系统)
- com.unity.inputsystem: 1.11.2 (新输入系统)
- com.unity.render-pipelines.universal: 14.0.11 (URP)
- com.unity.animation.rigging: 1.2.1 (动画绑定)
- com.unity.ai.navigation: 1.1.7 (导航系统)
- com.unity.burst: 1.8.26 (性能优化)
- com.unity.collections: 1.5.2 (高性能集合)
- com.unity.mathematics: 1.3.3 (数学库)
- com.cysharp.unitask (异步任务)
```

---

## 🎨 美术资源

### 角色模型
- **Anbi** (安比): 主角角色
- **BlackGirl**: 黑衣少女
- **Qianxiao**: 千晓
- **Katixiya**: 卡蒂西亚
- **Fuludelisi**: 芙露德莉丝
- **Tomorin**: 托莫林
- **Anon**: 阿农

### 动画资源
- `CLazyRunnerActionAnimPack`: 动作动画包
- `KAWAII_ANIMATIOMS_100`: 可爱动画包
- `Gril_GreatSword_AnimSet`: 大剑动画集

### 特效资源
- `Slash_effects`: 斩击特效
- `VFX_Klaus`: Klaus 特效包
- `Vfx`: 通用特效

### UI 资源
- `Modern UI Pack`: 现代 UI 包
- `ZZZUI`: 绝区零风格 UI

---

## 🔍 技术亮点

### 1. ECS 与 MonoBehaviour 混合架构
**优势**:
- ECS 处理高性能战斗逻辑
- MonoBehaviour 处理表现与交互
- 两者通过接口解耦，单向依赖

**实现**:
```csharp
// View → Logic 同步
ViewToLogicSyncSystem: 同步玩家/Boss位置到ECS

// Logic → View 同步
ViewSyncSystem: 同步ECS实体位置到GameObject

// 接口解耦
ICharacter: 统一角色接口
IDamageNumberService: 伤害数字服务接口
IDeathCallback: 死亡回调接口
```

### 2. 数据驱动的技能系统
- 所有技能配置为 ScriptableObject
- 支持可视化编辑
- 热更新友好
- 帧事件系统灵活强大

### 3. 双层 Buff 系统
- ECS 层处理数值计算（高性能）
- MonoBehaviour 层处理视觉效果（灵活）
- 两层协同工作，各司其职

### 4. 灵活的移动系统
- 支持两种移动模式切换
- Root Motion 精确控制
- 自定义 Root Motion 处理器

### 5. 完善的输入缓冲
- 技能输入缓冲
- 连击输入缓冲
- 避免输入丢失

### 6. 状态机 + Animancer
- 状态机管理游戏逻辑
- Animancer 管理动画播放
- 职责清晰，易于维护

---

## 📊 性能优化

### 1. ECS 架构优化
- 批量处理实体
- 缓存友好的数据布局
- 避免 GC 分配

### 2. 对象池
- 伤害数字对象池
- 特效对象池
- 掉落物对象池

### 3. Addressables 资源管理
- 异步加载
- 内存管理
- 资源热更新

### 4. Burst 编译
- 高性能数学运算
- SIMD 优化

### 5. 碰撞检测优化
- 空间网格划分 (`UniformGridMgr`)
- 分层碰撞检测 (`EnumGridChannel`)
- 避免全局遍历

---

## 🚀 开发规范

### 1. 命名规范
- **类名**: PascalCase (如 `PlayerController`)
- **方法名**: PascalCase (如 `HandleInput()`)
- **字段**: camelCase (如 `_playerEntity`)
- **属性**: PascalCase (如 `PlayerEntity`)
- **常量**: PascalCase (如 `DefaultMoveSpeed`)

### 2. 代码组织
- 按功能模块划分文件夹
- 每个系统独立命名空间
- 接口与实现分离

### 3. 注释规范
- 所有公共 API 必须有 XML 注释
- 复杂逻辑添加行内注释
- 使用 `/// <summary>` 描述类和方法

### 4. 配置管理
- 所有配置使用 ScriptableObject
- 配置文件统一放在 `Assets/Config/`
- 使用 Odin Inspector 增强编辑体验

---

## 🐛 已知问题与待优化

### 1. EnemyController 已注释
- 原有敌人控制器代码已注释
- 需要重构以适配新架构
- 当前使用 BossController 作为临时方案

### 2. 测试代码未清理
```csharp
// PlayerController.Init() 中的测试代码
if (Manager.LootDropManager.Instance != null)
{
    // 生成大量测试掉落物
    for (int i = 0; i < 9; i++)
        Manager.LootDropManager.Instance.SpawnWorldDrop(...);
}
```

### 3. BattleController 已废弃
- `Assets/Scripts/Battle/ECS/BattleController.cs` 已全部注释
- 已被 `BattleEcsRunner` 替代

### 4. 缺少网络同步
- 当前仅支持单机
- 需要添加网络同步层

### 5. 存档系统待完善
- 当前存档功能基础
- 需要添加云存档支持

---

## 📈 未来规划

### 短期目标
1. 重构 EnemyController，适配 ECS 架构
2. 完善 Boss AI 行为树
3. 添加更多技能与连招
4. 优化打击感（顿帧、震屏、音效）
5. 完善 UI 系统

### 中期目标
1. 添加装备系统
2. 实现天赋树
3. 添加副本系统
4. 实现成就系统
5. 优化性能（移动端适配）

### 长期目标
1. 多人联机支持
2. 服务端开发
3. 热更新系统
4. 数据分析与埋点
5. 商业化功能

---

## 🎯 总结

这是一个**架构清晰、技术先进、扩展性强**的 Unity ARPG 项目。核心亮点包括：

1. **混合架构**: ECS + MonoBehaviour，兼顾性能与开发效率
2. **数据驱动**: 技能、Buff、角色配置全部数据化
3. **高性能战斗**: ECS 架构保证战斗逻辑高效运行
4. **灵活的动画系统**: Animancer + Root Motion，代码驱动动画
5. **完善的技能系统**: 支持连击、帧事件、输入缓冲
6. **双层 Buff 系统**: 数值与表现分离，各司其职
7. **现代化工具链**: Addressables、New Input System、URP

项目当前处于**核心系统搭建完成，内容填充阶段**，具备良好的扩展性和维护性，适合继续深度开发。

---

**文档生成时间**: 2026-03-03
**项目版本**: Unity 6000.3.8f1
**文档版本**: v1.0
